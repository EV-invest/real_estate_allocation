[application]
name = "real_estate_allocation"

[web.app]
title = "Real Estate Allocation"
# Zone mount: the conductor serves this app under evinvest.ltd/rea, so
# pages, /assets and server-fn /api all nest under the prefix (compile-time in
# release). Direct origin root now 404s; /health + /api/embed/* stay at root
# (outer axum router, main.rs).
base_path = "rea"

[web.resource.dev]
style = []

[web.resource]
style = []
